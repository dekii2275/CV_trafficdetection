import type { VehicleType } from "./types";

export const VEHICLE_TYPES: VehicleType[] = ["car", "truck", "motor", "bus"];

export const API_ROUTES = {
    stream: "/api/stream",
    upload: "/api/upload",
};