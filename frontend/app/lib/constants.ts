import type { VehicleType } from "./types";

export const VEHICLE_TYPES: VehicleType[] = ["car", "truck", "bike", "bus"];

export const API_ROUTES = {
    stats: "/api/stats",
    stream: "/api/stream",
    chart: "/api/chart",
    upload: "/api/upload",
};
